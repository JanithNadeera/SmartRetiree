import 'package:flutter/material.dart';

const iconThemeLight = IconThemeData(
  size: 24,
  color: Color(0xFF0A090E), // Same as onSurface in light theme
  opacity: 0.87,
);

const iconThemeDark = IconThemeData(
  size: 24,
  color: Color(0xFFFAFAFA), // Same as onSurface in dark theme
  opacity: 0.87,
);
